schema: http://localhost:4000
overwrite: true
config:
  contextType: ./types.d.ts#IContext
generates:
  ./src/types/schema.d.ts:
    plugins:
    - add
    - time
    - typescript-common
    - typescript-server
    - typescript-resolvers
