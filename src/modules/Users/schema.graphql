#import Error from "../../common.graphql"

type Query {
  hello: String!
}

type Mutation {
  register(email: String!, password: String!): [Error!]
  login(email: String!, password: String!): AuthResponse!
  logout: Boolean!
}

type AuthResponse {
  error: [Error!]
  token: String
}